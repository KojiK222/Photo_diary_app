= render partial: "layouts/header"
%body
  .left-button
    = link_to "ユーザー一覧に戻る", "/users/index",class:"edit-link"
  %h4.talkrooms-title 気になる同士
  - @entries.each do |e|
    .talkuser-name
      - if e.user == current_user
        .talkroom__current-user
          .talkroom-user
            = attachment_image_tag current_user, :profile_image, fallback: "no-image.png", class: "talkroom__user-icon"
          %a.talkrooms-user-link{:href => "/users/#{e.user.id}"}
            = e.user.username
      - else
        .talkroom__follow-user
          .talkroom-user
            = attachment_image_tag @user, :profile_image, fallback: "no-image.png", class: "talkroom__user-icon"
          %a.talkrooms-user-link{:href => "/users/#{e.user.id}"}
            = e.user.username
  %hr/
  .chats
    .chat
      - if @messages.present?
        - @messages.each do |m|
          .chat-box
            .talkroom-user
              = attachment_image_tag @user, :profile_image, fallback: "no-image.png", class: "talkroom__user-icon"
            .chat-hukidashi
              %strong= m.content
              %br/
              = m.created_at.strftime("%Y-%m-%d %H:%M")
    .posts
      = form_for @message do |f|
        = f.text_field :content, placeholder: "メッセージを入力して下さい" , size: 70, class:"form-text-field"
        = f.hidden_field :talkroom_id, value: @talkroom.id
        = f.submit "投稿",class: 'form-submit'

        -# .talkroom__user-name
        -#   = link_to user_path(e.user.id)
